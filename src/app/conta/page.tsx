import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Minha conta',
};

export default function ContaPage() {
  return (
    <section>
      <h1>Conta</h1>
    </section>
  );
}
